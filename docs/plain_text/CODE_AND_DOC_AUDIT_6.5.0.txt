# Code + Documentation Audit — GRS Observatory v6.5.0

**Date:** 2026-07-19  
**Tree:** `GRS_Observatory/`  
**Method:** Live file inventory, symbol/wiring grep, version cross-check, full `unittest` suite  
**Scope:** Changes since the 6.1-era product (Champion / SUPERDUPER / job_finalize / doc rewrite) vs docs

---

## 1. Executive summary

| Area | Grade | Notes |
|------|------:|-------|
| Code product stack (desktop Process) | **A−** | Champion → publish → SUPERDUPER wired; post-dual re-card |
| Server parity | **B+** | `job_finalize` called after gold on `/api/process` (was gap; fixed) |
| Science integrity vs older audits | **B+** | Time fail-closed, map 180°, GS primary not soup, oriented inverse retained |
| Documentation accuracy (operator Book) | **A−** | Aligned to 6.5 after this audit’s version fix |
| Professor essay | **B** | Narrative updated; module line tables still ~6.1 historical |
| Test coverage vs surface area | **C+** | 37 tests pass; no full Process e2e, no server e2e |
| Version consistency (pre-fix) | **B−** | Drifted (6.4.x / 6.4.2 / 6.5.0) — **fixed in this audit** |

**Bottom line:** Code and the **user Book / PLATEAU / README** describe the same product. The **essay module inventory tables are stale** (documented). Automated suite is green (37 OK). Residual risk is incomplete server synthetic finalize and thin integration tests.

---

## 2. Version & inventory (live)

| Item | Value |
|------|--------|
| `VERSION` | **6.5.0** |
| `pyproject.toml` | **6.5.0** |
| `app/*.py` modules | **41** |
| Total `app/*.py` lines | **~30 921** |
| Largest module | `grs_complete_system.py` **4534** lines (was ~10k) |
| New core modules | `champion_measure.py` (1401), `superduper.py` (191), `job_finalize.py` (182), `winjupos_plus.py` |
| Tests | **37 passed** (~89 s), warnings: empty `nanmean` in all_methods* |

---

## 3. Code change map (what was added/changed)

### 3.1 Product / science (high impact)

| Module | Change |
|--------|--------|
| `champion_measure.py` | Ultimate automated path: multi-isophote limb, dark-core score, multi-size template, jackknife σ, refine bootstrap, dual-channel, nav stability, **13-gate** `UNBEATABLE_AUTO`, full σ budget |
| `publish_primary.py` | Hierarchy: UNBEATABLE/Champion → GS-MAP → GS-BARY → pipeline; soup/SOTA not primary; φ_g export; champion σ on publish |
| `superduper.py` | One-page `SUPERDUPER_BEST_ANSWER.*` + `REPORT_THIS_ONE_LINE.txt` |
| `winjupos_plus.py` | Desk-parity card (φ_g, EW, desk score) |
| `job_finalize.py` | Single attach path: Champion + publish + WinJUPOS+ + SUPERDUPER + `JOB_COMPLETE.*` |
| `desktop_pipeline.py` | Uses finalize; post-dual re-publish + SUPERDUPER |
| `server.py` | `/api/process` calls finalize after gold (parity) |
| `precision_engine.py` | Oriented `px_to_lonlat`; denser limb when high ray count; planetographic helpers |
| `all_methods.py` | Lon-rel interpolation (no wrap-lerp 180° bug) |
| `ephemeris_pro.py` | SPICE NaN CM handling; second cache key; SPICE↔Horizons ΔCM; analytical warning |
| `spice_auto.py` | Distance-only → CM NaN (not 0) |
| `nasa_compare.py` | Parse Horizons or fail (no fake AU success) |
| `gold_standard.py` | Classic scatter not overwritten by soup |
| `sota_accuracy.py` | No gold primary overwrite; reduced pipeline hijack |
| `grs_complete_system.py` | Dead bulk stripped (~5.8k lines removed) |
| `result_report.py` / `desktop_app.py` | SUPERDUPER / champion grades in report & metrics |

### 3.2 Tests added

| File | Covers |
|------|--------|
| `test_champion.py` | Champion run, analytical CM, publish prefer champion / UNBEATABLE label |
| `test_superduper.py` | Card build |
| `test_science_p0_fixes.py` | Lon-rel interp, planetographic, flips, CM trust |
| `test_winjupos_plus.py` | Desk card |
| Existing | accuracy_gates, human_choice, desktop_wiring, accuracy_smoke |

### 3.3 Docs changed

| File | Status |
|------|--------|
| `docs/GRS_OBSERVATORY_BOOK.md` | **Rewritten** for Champion / SUPERDUPER / gates / plateau |
| `docs/PROFESSOR_TECHNICAL_ESSAY.md` | Abstract, §1–3, §2.5, glossary, addendum revised |
| `docs/PLATEAU.md` | **New** — why further soup has low ROI |
| `docs/reference/02_ARCHITECTURE.md` | Process path + job_finalize |
| `docs/reference/03_GLOSSARY.md` | New terms |
| `README.md`, `PROJECT_MAP.md` | Point to SUPERDUPER + 6.5 |
| Essay pack under `grs essay and guide/` | Synced guide + essay (+ PLATEAU.txt) |

---

## 4. Wiring verification (code)

| Check | Result |
|-------|--------|
| Desktop Process uses `job_finalize` | **Yes** |
| Desktop post-dual SUPERDUPER refresh | **Yes** |
| Synth uses finalize then final SUPERDUPER | **Yes** |
| Server `/api/process` uses finalize | **Yes** |
| Ultimate gate count in code | **13** keys in `checks` |
| PLATEAU / Book say 13 gates | **Yes** (aligned) |
| `publish_primary` prefers `unbeatable_auto` | **Yes** |

### Ultimate gates (authoritative from code)

1. `trusted_cm`  
2. `cm_crosscheck` (SPICE↔Horizons \|ΔCM\| ≤ 0.35° if both)  
3. `finite_pos`  
4. `lat_core`  
5. `no_map_edge`  
6. `limb_stable`  
7. `def_tight`  
8. `dark_core`  
9. `nav_stable`  
10. `dual_agree`  
11. `score_high`  
12. `gs_or_strong`  
13. `sigma_sky_tight`  

---

## 5. Documentation vs code — findings

### Fixed in this audit pass

| ID | Issue | Fix |
|----|--------|-----|
| D1 | Book header said **6.4.x** while `VERSION` is **6.5.0** | Book → 6.5.0 |
| D2 | Essay header/addendum **6.4.2** | → 6.5.0 + job_finalize mention |
| D3 | Architecture still **6.4.x** / incomplete finalize path | → 6.5.0 + `job_finalize` steps |

### Remaining gaps (not blocking self-use)

| ID | Severity | Finding |
|----|----------|---------|
| D4 | **P2** | Essay **§4+ module dumps / line tables** still ~6.1.0 inventory (10350-line monolith etc.). Addendum warns; tables not regenerated. |
| D5 | **P2** | `docs/reference/01_FEATURES.md` still auto-generated 6.1-era feature dump — not updated for Champion/SUPERDUPER. |
| D6 | **P2** | `docs/FULL_CODE_AUDIT_6.1.0.md` is historical; should not be read as current (Book already says so). |
| D7 | **P3** | Essay guide folder: essay txt may lag until re-synced after this version bump (re-sync recommended after edit). |
| D8 | **P3** | SECURITY.md still “v6.0+” — still accurate enough, not product narrative. |

### Doc claims verified true

- Publish = SUPERDUPER / hierarchy, soup scatter only  
- UNBEATABLE_AUTO honesty clause (in-app, not HST)  
- SUPERDUPER does not remeasure  
- Time refuse silent `now` on process paths  
- Prefer red / GS-MAP philosophy  

---

## 6. Residual code risks (not re-audited to P0 depth)

| ID | Severity | Topic |
|----|----------|--------|
| C1 | **P1** | Server **synthetic** path may still be hand-rolled (research_grade) without full finalize — confirm if web synth lacks SUPERDUPER. |
| C2 | **P1** | Champion is expensive (multi-limb + bootstrap + dual + nav jitter); fine for quality, not for 1 Hz loops. |
| C3 | **P2** | `all_methods` empty-slice RuntimeWarnings on smoke synth. |
| C4 | **P2** | Dual human official can still override UNBEATABLE (intentional WinJUPOS-style; document is clear). |
| C5 | **P2** | No automated test runs full `run_process_full` or server worker (only unit/wiring/smoke). |
| C6 | **P3** | License fail-open paths remain for local EVAL / `GRS_LICENSE_OPEN`. |

---

## 7. Tests

```
Ran 37 tests in ~89s
OK
```

Smoke: accuracy_smoke synthetic may emit `nanmean` warnings — non-fatal.

---

## 8. Recommended next actions (if any)

1. **Optional:** Regenerate or delete stale `docs/reference/01_FEATURES.md` and essay line-count tables.  
2. **Optional:** Wire `job_finalize` into server **synthetic** path for full parity.  
3. **Optional:** Re-sync `grs essay and guide/*.txt` after every VERSION bump.  
4. **Do not:** Add more soup methods for “accuracy” — follow `docs/PLATEAU.md`.  

---

## 9. Verdict

| Question | Answer |
|----------|--------|
| Do code changes match the operator Book? | **Yes** (after version string fix) |
| Do code changes match PLATEAU/README? | **Yes** |
| Is the professor essay trustworthy as architecture truth? | **Yes for §1–3 / §2.5 / glossary**; **no** for old line-count tables |
| Is the product shippable for self-use science nights? | **Yes**, with SPICE/WJ CM discipline |
| Are we at a code plateau for single-frame auto GRS? | **Yes** — further ROI is data/methods outside this app |

**Audit status:** PASS with residual P2 doc debt (essay inventory / features dump) and P1 optional server-synth finalize check.
