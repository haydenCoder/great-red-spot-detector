# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# macOS
.DS_Store
**/.DS_Store

# Local runtime (regenerated when you run the app)
app/outputs/
app/uploads/
app/logs/
app/ssd_cache/
app/nasa_cache/
app/owner_access/
app/session.json
app/admin_session.json

# Owner device telemetry stays local (privacy)
OWNER_SHARED_LOGS/

# Packaging
dist/
build/
*.app/
*.spec

# Editor
.idea/
.vscode/
*.swp

# local runtime accounts
app/accounts.json
app/session.json
app/admin_session.json

# NOTE: training weights + checkpoint + Train_SPIRE_Background.command
# are TRACKED on purpose (restored from past project history).

# Atomic-write temp debris (see DEFECT J in docs/AUDIT_GEOMETRY_AND_SMOKE_6.5.1.md)
*.tmp.npz
app/models/*.tmp.*

# Pre-refactor backups
*.bak_before_deadstrip

# Accuracy campaign outputs (regenerated; can be large)
runs/
image-search/
app/native/*.so
