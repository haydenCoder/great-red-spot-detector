# Past job exports (restored from local history)

Text/JSON products from earlier Process / factory / ephemeris runs, restored from the old project tree.
Full binary stacks and large synthetics stay under local `app/outputs/` (not in git).

The verified 2026-01-09 case is also under `../verified_case_2026-01-09/`.
